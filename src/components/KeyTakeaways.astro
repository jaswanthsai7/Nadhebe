---
interface Props {
  items: string[];
}
const { items } = Astro.props;
---

<div class="card p-6">
  <p class="mb-4 flex items-center gap-2 text-meta font-sans font-semibold uppercase tracking-wider text-muted dark:text-muted-dark">
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" class="h-3.5 w-3.5">
      <path d="M12 2v4M12 18v4M4.9 4.9l2.8 2.8M16.3 16.3l2.8 2.8M2 12h4M18 12h4M4.9 19.1l2.8-2.8M16.3 7.7l2.8-2.8"></path>
    </svg>
    Key takeaways
  </p>
  <ul class="space-y-3">
    {items.map((item) => (
      <li class="flex gap-3 text-body leading-relaxed text-ink2 dark:text-ink2-dark">
        <span class="mt-1.5 h-1 w-1 shrink-0 rounded-full bg-accent dark:bg-accent-dark"></span>
        <span>{item}</span>
      </li>
    ))}
  </ul>
</div>
